server:
  port: 8080
spring:
  application:
    name: server
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: project123
    url: jdbc:mysql://localhost:3306/invoicesys
    username: project
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
    show-sql: true